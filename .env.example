# NetSuite MCP Bridge 環境變量配置
# 複製此文件為 .env 並填入你的實際值

# NetSuite Account ID (會自動轉為小寫)
NETSUITE_ACCOUNT_ID=td3018275

# NetSuite OAuth Client ID (從 NetSuite Integration 記錄獲取)
NETSUITE_CLIENT_ID=your_client_id_here

# NetSuite OAuth Client Secret (可選，Confidential Client 需要)
NETSUITE_CLIENT_SECRET=your_client_secret_here

# 應用基礎 URL (本地開發用 localhost，生產環境用 Zeabur URL)
APP_BASE_URL=http://localhost:3000

# NetSuite OAuth Scope (可選，默認為 rest_webservices)
NETSUITE_SCOPE=rest_webservices

# OAuth Callback Port (可選，默認為 9090)
OAUTH_CALLBACK_PORT=9090
